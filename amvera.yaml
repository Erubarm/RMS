meta:
  environment: nodejs
  toolchain:
    name: node
    version: 20

build:
  npmBuild:
    buildCommand: "npm run build:all"

run:
  persistenceMount: /mnt/data
  containerPort: 3000
  command: "npm run start:prod"
